- if:
  - path: /etc/crio/openshift-pull-secret
    op: [created, updated, removed]
  run: systemctl restart microshift

- if:
  - path: /etc/microshift/observability/opentelemetry-collector.yaml
    op: [created, updated, removed]
  run: systemctl restart microshift-observability